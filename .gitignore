# ── Sensitive — API keys ──────────────────────────────────
.env.local
.env

# ── Raw dataset (large, not for source control) ───────────
*.csv

# ── Large generated HTML map files (>5 MB each) ──────────
approach_A.html
approach_B.html
bengaluru_hotspots_interactive_map.html

# ── Scratch / exploratory JSON outputs ────────────────────
explore_out.json
explore_ps2_out.json

# ── Exploratory / intermediate notebooks ─────────────────
# cluster_final.ipynb is the canonical notebook — keep it.
cluster.ipynb
compare.ipynb
PS1.ipynb

# ── Jupyter ───────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── Python ────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# ── Node / Vercel ─────────────────────────────────────────
node_modules/
.vercel/

# ── Export JSON (large, regenerated by export_analysis.py) ─
# Keep exports/ in source control if <10 MB total, else uncomment:
# exports/
# site/exports/

# ── OS ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*~

# ── Local dev server ─────────────────────────────────────
dev-server.js
node_modules/
